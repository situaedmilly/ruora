# SELFHTML — HBC LINEAGE ADOPTION 001 · DOCTRINE ONLY

**ACT** `AUTHORIZE_SELFHTML_HBC_LINEAGE_ADOPTION_001_DOCTRINE_ONLY`
**PREDECESSOR GATE** `FOUNDER_ADJUDICATION_SELFHTML_HBC_LINEAGE_EXTRACTION_001` — Founder-adjudicated **PASS**
**RECORDED** 2026-08-14 · session `bd059265`
**CLASS** Doctrine adjudication record. This document ratifies inheritance law. It creates no repository, copies no file, selects no path, mutates no compiler, implements no schema, and projects no runtime.
**RECORD CUSTODY DISCLAIMER** This file's location under `governance/` is a record-custody convenience for the adjudication act itself. It is **not** a canonical-path, custody-format, or repository decision for SELFHTML, all of which remain UNRESOLVED (§8).

---

## 0. Disk witnesses taken before recording

All witnesses read-only, taken 2026-08-14 by session `bd059265`:

```
HBC repository            ~/RUORA/projects/hbc-html
PROTECTED_HBC_BASELINE    e350205f12c140b8216090b935f874cbbc454dee   present · "HBC-HTML-v0.1" · 2026-08-12
CURRENT_HBC_HEAD          4792500b1b579b85454ec0e76a07d7cf1a2c9e83   "Add HBC command transit workflow" · 2026-08-13
BASELINE_DISPOSITION      PRESERVE_SUCCESSOR_QUARANTINED             (standing carried, not altered here)
AUTHORITY_STANDING        UNRESOLVED                                 (standing carried, not altered here)
WORKING TREE              clean except pre-existing untracked output/ — not touched by this act
HBC MUTATION BY THIS ACT  NONE
```

**Repository self-declaration witness (grounds Precision Law 1).** `README.md` line 1: *"HBC HTML — Hyperbolic Build Chamber for HTML Realms."* Line 5: *"A compiler."* `package.json` name: `hbc-html`. A grep across `README.md`, `doctrine/`, and `src/` for any SELF-identity self-declaration (`SELF identity`, `HBCSELF`, `SELFHTML`) returns **zero hits**. The disk says compiler/chamber. It does not say SELF.

**Extraction-report durability witness (evidence gap, preserved).** A RUORA-wide search (`grep -rln "SELFHTML" / "HBCSELF"` over all `*.md`, excluding `node_modules`) returns **zero hits** prior to this file. The extraction gate's findings are therefore **not durably recorded on disk**; they exist in session lineage and in the Founder's authorizing restatement.

```
EXTRACTION_REPORT_DURABLE_RECORD = ABSENT
EVIDENCE BASIS OF THIS ADOPTION  = FOUNDER RESTATEMENT IN THE AUTHORIZING ACT
                                   + INDEPENDENT DISK WITNESSES ABOVE
FOUNDER_RESTATEMENT ≠ DURABLE_EXTRACTION_ARTIFACT
```

Per the standing law that a ratification binds an evidence lineage: this adoption binds to the Founder's stated dispositions and to the disk witnesses above — not to an extraction artifact this record cannot cite by digest. If the extraction report is later durably recorded, it joins this lineage by appending; this record is not edited to pretend it was always present.

---

## 1. The two precision laws

**Precision Law 1 — lineage name is not self-declaration.**

```
FounderLineageName(HBCSELF) ≠ RepositorySelfDeclaration
```

`HBCSELF` is the Founder's lineage designation for the existing HBC system. The hbc-html repository presently declares itself a compiler/chamber and nothing more (§0 witness). This law stands **until a later identity gate deliberately reconciles it**. No document may retroactively make HBC say "I am a SELF" merely because its function is now recognized that way.

**Precision Law 2 — semantic inheritance is not byte copying.**

```
INHERIT_EXACT ≠ COPY_BYTES_EXACT
```

`INHERIT_EXACT` means **semantic law survives unchanged**. Whether SELFHTML imports a module, wraps it, vendors it, depends on it, or reimplements the same contract is a later implementation decision, expressly not made here.

---

## 2. Rulings ratified (Founder act, recorded verbatim in substance)

1. **SELFHTML is a new SELF identity candidate.** `SELFHTML ≠ HBC`. `SELFHTML ≠ Claude`. `SELFHTML ≠ Codex`. `SELFHTML ≠ AgentBridge`.
2. **HBCSELF is the Founder lineage designation** for the existing HBC system. This does not retroactively assert that the hbc-html repository presently declares a SELF identity (Precision Law 1, disk-witnessed §0).
3. **SELFHTML is `DERIVED_FROM` HBCSELF.**
4. **`DERIVED_FROM` does not mean identity equivalence.**
5. **`INHERIT_EXACT` means semantic inheritance, not mandatory byte copying** (Precision Law 2).
6. **The extraction's four inheritance classes are ratified:** `INHERIT_EXACT` · `INHERIT_WITH_GENERALIZATION` · `HBC_ONLY` · `UNRESOLVED` (membership in §3–§5, unresolved set in §8).
7. **The HBC proof membrane is ratified as SELFHTML's inherited proof ancestor:** validate · witness · differentiate · prove · deterministic digesting · fail-closed contract behavior · negative-control methodology · explicit `NOT_WITNESSED` behavior.
8. **The three-plane separation is ratified:** `MEANING` ≠ `REALIZATION` ≠ `PROOF`.
9. **SELFHTML additionally requires an `ADJUDICATION` boundary** — a fourth plane HBC does not carry.
10. **The non-collapse laws are ratified:**
    ```
    source evidence      ≠ render evidence
    render evidence      ≠ design judgment
    witness              ≠ adjudication
    artifact existence   ≠ canonical standing
    capability           ≠ authority
    runtime projection   ≠ SELF identity
    ```
11. **Typed differentiation is ratified as required future semantics** (§6).
12. **All UNRESOLVED extraction findings are preserved as unresolved** (§8). This adjudication closes none of them.

---

## 3. INHERIT_EXACT — the normative HBC inheritance kernel

Semantic laws that must survive into SELFHTML unchanged (as law; embodiment open per Precision Law 2):

- fail-closed contract validation — invalid input stops compilation, never degrades it;
- declared-but-unrealized and realized-but-undeclared both fail;
- deterministic digests; no wall-clock leakage into proof surfaces;
- artifact corruption detection;
- static authority refusal — the artifact does not grant itself authority it was not given;
- primitive-binding integrity;
- injective glyph/label rules where applicable;
- refusal to overclaim — explicit `NOT_WITNESSED` rather than fabricated browser/render/design claims;
- artifact digest scope discipline;
- negative-control testing methodology.

**The proof genome, inherited as a chain, not as files:**

```
Contract → Validate → Primitive binding → Artifact digest → Witness → Differentiate → Proof
```

## 4. INHERIT_WITH_GENERALIZATION

Concepts that survive, whose HBC-specific schemas may NOT be inherited as universal law: packet schema · frequency representation · section semantics · witness breadth · differentiation · transit custody · reverse diagnosis · output witness shape.

## 5. HBC_ONLY

Not inherited: `hbc` CLI identity · commerce taxonomy · current fixtures · current sections · current generated outputs · workflow naming · the premise that static HTML is the whole target.

---

## 6. Typed differentiation — required future semantics

One PASS may not launder a realm. Differentiation is a typed vector; each axis carries its own status:

```
Δ_SELFHTML = (Δ_semantic, Δ_structural, Δ_visual, Δ_responsive, Δ_interaction, Δ_authority, Δ_evidence)
```

Statuses per axis include at least `PASS`, `FAIL` (with findings), `NOT_WITNESSED`, `INCOMPLETE`. No axis's status is derivable from another's. The exact schema is an implementation decision — **not made here** (§8).

## 7. Observation classes — the inherited honest null, made constitutional

HBC's witness explicitly cannot prove browser render, responsive reflow, or design quality. That null is inherited as **constitutional law**, not as a temporary limitation:

```
SOURCE_OBSERVATION ≠ DOM_OBSERVATION ≠ RENDER_OBSERVATION ≠ DESIGN_ADJUDICATION
```

SELFHTML must never perform the inversion *"source looks right, therefore rendered experience is right."* `SourceWitness ≠ RenderWitness`; `RenderWitness ≠ DesignAdjudication`. Representation, observation, evidence, and judgment do not collapse into one object.

---

## 8. Preserved UNRESOLVED items — none closed here

- canonical path for SELFHTML;
- custody format;
- browser observation engine;
- adjudication schema;
- mutation-proposal schema;
- primitive completeness;
- INSELFACTION relation;
- manifestation topology (new repository vs HBC dependency vs shared-library extraction vs other lineage-preserving architecture).

## 9. Gaps and near-contradictions discovered while recording — disclosed, not resolved

1. **The extraction report itself is unrecorded** (§0 witness). This adoption's evidence basis is the Founder's authorizing restatement plus independent disk witnesses. Not a contradiction of any ruling — but a provenance limitation every later gate should know it inherits.
2. **The lineage ancestor digest is unpinned.** `DERIVED_FROM HBCSELF` — at which state? The proven claims (75/75 tests, fail-closed behavior, v0.1 membrane) were established at `PROTECTED_HBC_BASELINE e350205f…`; the successor head `4792500b…` carries `AUTHORITY_STANDING = UNRESOLVED`. Ruling 7 inherits the proof membrane; it does not say from which digest the membrane's semantics are read. This record does **not** decide it. It is surfaced as a required Founder decision (§10), because an unpinned ancestor invites exactly the drift this lineage law exists to prevent.
3. **No contradiction found between the twelve rulings and disk state.** Precision Law 1's factual predicate is disk-verified. The exclusions were honored: zero HBC mutations, zero file copies, no repository created, no path selected, no schema implemented.

## 10. Exact next Founder decisions required

1. **Ancestor digest pin** — is SELFHTML `DERIVED_FROM` HBCSELF-at-`e350205f…` (the proven baseline), HBCSELF-at-`4792500b…` (the quarantined successor), or HBCSELF-as-lineage with the baseline as the semantic reference? (Gap §9.2 — recommended to settle **before** manifestation topology, since topology choices read the ancestor.)
2. **Manifestation topology** — new repository vs HBC dependency vs shared-library extraction vs other lineage-preserving architecture. Expressly not chosen here.

---

## 11. Exclusions honored

NO HBC mutation · NO file copying · NO SELFHTML repository creation · NO SELFHTML path selection · NO compiler implementation · NO schema implementation · NO Claude/Codex projection · NO AgentBridge change · NO browser automation build · NO canonical custody decision · NO git staging/commit/push of this record (working-tree file only; custody of the record is itself a later act).

**STOP. Doctrine adjudication complete.**

---

*HBCSELF is not code being cloned. It is proven epistemic machinery being inherited. SELFHTML adds governed manifestation around that machinery without weakening the critic that made HBC valuable.*
