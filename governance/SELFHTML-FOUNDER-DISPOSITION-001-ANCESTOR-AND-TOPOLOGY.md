# SELFHTML — FOUNDER DISPOSITION 001 · ANCESTOR PIN + MANIFESTATION TOPOLOGY

**CLASS** Founder disposition record. Two rulings, durably recorded as the precondition the Founder set for authorizing `SELFHTML_REALITY_CONTRACT_SEMANTICS_001`.
**RECORDED** 2026-08-14 · session `bd059265`
**PREDECESSORS** `SELFHTML-HBC-LINEAGE-ADOPTION-001-DOCTRINE.md` (`f3ed35a2…5e12`) · `SELFHTML-REALITY-CONTRACT-DESIGN-PACKET-001.md` (`2cfdecd1…1f85`)
**RESOLVES** Adoption doctrine §10's two queued Founder decisions.

---

## RULING 1 — Ancestor digest pin

```
Ancestor(SELFHTML) = HBC @ e350205f12c140b8216090b935f874cbbc454dee   (PROVEN_BASELINE)
NOT                = HBC @ 4792500b1b579b85454ec0e76a07d7cf1a2c9e83   (QUARANTINED successor)
```

Canonical lineage block:

```yaml
lineage:
  ancestor:
    system: HBC
    commit: e350205f12c140b8216090b935f874cbbc454dee
    standing: PROVEN_BASELINE
  observed_successor:
    commit: 4792500b1b579b85454ec0e76a07d7cf1a2c9e83
    standing: QUARANTINED
    inheritance_authority: NONE
```

A quarantined successor may contain useful later discoveries, but quarantine is precisely the statement that its standing has not earned inheritance authority. Future adjudication may promote **specific deltas** from `4792500b` without rewriting SELFHTML's birth ancestor.

**Laws recorded:**

```
ObservedLater      ⇏ CanonicalAncestor
QuarantinedArtifact ⇏ InheritanceAuthority
```

## RULING 2 — Manifestation topology

**Independent canonical system, not a fork.** Designated location: `~/RUORA/systems/selfhtml/` — `DERIVED_FROM` HBCSELF with explicit lineage declarations. **No wholesale HBC fork/copy**, because the inheritance matrix holds four fundamentally different mechanisms (`INHERIT_EXACT` = semantic law · `INHERIT_WITH_GENERALIZATION` = successor semantics · `HBC_ONLY` = excluded · `UNRESOLVED` = may not enter implementation as assumption), and a filesystem fork would physically import all four before the semantic distinction could govern them. The repository is born from lineage declarations and contracts, then admits implementation deliberately.

The internal directory tree dictated by the Founder (governance/ · doctrine/ · ontology/ · contracts/ · ir/ · compiler/ · witness/ · differentiate/ · proof/ · tests/ · fixtures/) is **CANDIDATE TOPOLOGY, not authorization to create it**. Repository genesis is sequence step 5, after the semantics hostile review.

## RULING 3 — Semantics gate authorization

`SELFHTML_REALITY_CONTRACT_SEMANTICS_001` is **authorized upon durable recording of Rulings 1–2** (this file). Its mandatory scope, as expanded by the Founder: typed status strata · event→proposition inference law · semantic-relation × epistemic-standing orthogonality · RealityContract · AuthorityContract · EvidenceContract · DifferentiationContract · state ontology · capability ontology · transition ontology · proof obligations · compiler boundaries.

Two recorder-found defects are folded into that scope as mandatory objects:

1. **Typed status strata, not one vocabulary.** `PASS/FAIL/INCOMPLETE` vs `VERIFIED` are probably different types (observation result ≠ governance verdict — the SELFagent Witness lesson). At least three strata: observation status (`OBSERVED / NOT_OBSERVED / NOT_WITNESSED / OBSERVATION_FAILED`) · proof-obligation result (`PASS / FAIL / INCOMPLETE`) · epistemic standing (`VERIFIED / DECLARED / INFERRED / DEFERRED / REJECTED`). Without the strata, PASS and VERIFIED become competing words for "good" — semantic laundering.
2. **The SELFDARD mapper needs an inference object.** `browser.section_visible → SELF.identity.revealed` may not be an untyped transformation. Required shape: `O —r→ P` (observation, inference rule, proposition), with output standing `INFERRED`; a standing of `VERIFIED` on a cognitive proposition (e.g. `visitor.recognized_identity`) is **unlawful** absent evidence actually capable of establishing recognition. `EVENT → OBSERVATION → PROPOSITION → INFERENCE → STANDING`, never `EVENT → whatever semantic state sounds desirable`.

## RULING 4 — Standing-axis pattern: candidate, not law

The same structural requirement has now appeared independently in RC-0001, OSM, and SELFHTML:

```
SemanticMeaning ⊥ EpistemicStanding
```

(WHAT is asserted must remain independent of HOW established it is.) Recorded as **`SHARED_ONTOLOGY_EXTRACTION_CANDIDATE`** — three independent manifestations justify investigation, **not** silent establishment of ecosystem law. Extraction into a shared ontology during SELFHTML semantics is expressly prohibited (regress guard).

## The Founder's sequence

```
1. HBC ancestor pin                      ← THIS RECORD
2. SELFHTML manifestation-topology ruling ← THIS RECORD
3. SELFHTML_REALITY_CONTRACT_SEMANTICS_001   ← authorized next
4. semantics hostile review
5. canonical SELFHTML repo genesis
6. ontology/contracts
7. compiler skeleton
8. HBC proof inheritance
9. first compiled reality
```

## Boundary

This record: one working-tree file, unstaged. No repository created, no HBC mutation, no shared-ontology extraction, no schema implementation.
