# Python-generated cache
__pycache__/
*.pyc

# macOS-generated metadata
.DS_Store

# Future secret-bearing files
.env
.env.*
*.pem

# Isolated project chambers — each has its own Git repository
projects/

# OURSELF preservation / quarantine infrastructure (benign, persistent, kept
# on disk but not tracked into the doctrine repo). Audited 2026-06-15:
# home-git preservation bundle, extracted OURSELF bridge system, quarantined
# home .git. No secret-bearing files (only a .env.example template).
backups/
systems/
quarantine/

# Local secrets
.env
.env.*
!.env.example

# AgentBridge operational evidence store; governed as non-Git runtime
# evidence by LIMITED_SELFLAUNCH_OPERATING_BOUNDARY.md
/runtime/agentbridge/
