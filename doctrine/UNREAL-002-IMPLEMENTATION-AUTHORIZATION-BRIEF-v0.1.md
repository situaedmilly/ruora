# UNREAL-002 Implementation Authorization Brief v0.1

## 1. Prime thesis

UNREAL-002 is not permission to open the entire Unreal lane. It is permission to define the first controlled implementation incision, with explicit machine, file, secret, artifact, rollback, and verification boundaries.

This brief prepares UNREAL-002 for Human_TURN review.

It does not complete UNREAL-002.

It does not authorize Unreal installation, Unreal project creation, remote worker execution, deployment, cloud provisioning, remote access configuration, UWebBrowser implementation, Pixel Streaming, public release, Bubble mutation, schema mutation, app implementation, package installation, API calls, remotes, pushes, worktrees, agents, or PRs.

## 2. Current state

- UNREAL-001 selected the worker topology.
- Selected topology: Dedicated Windows workstation or Shadow/Parsec-style remote workstation, with Local Mac remaining command console.
- Local Mac remains command console and proof authority.
- Worker machine carries Unreal storage, editor load, GPU/render pressure, compile pressure, packaging pressure, and simulation pressure.
- UNREAL-002 remains blocked until Human_TURN accepts this brief.
- Repo remains proof body.

## 3. Doctrine dependencies

This brief inherits from:

- `doctrine/UREEL-REALM-VISION-v0.1.md`
- `doctrine/OURSELF-REMOTE-BUILD-NODE-CONTROL-PLANE-v0.1.md`
- `doctrine/REMOTE-WORKER-SELECTION-MATRIX-v0.1.md`
- `doctrine/FREED-SELF-14-DAY-CHAMBER-MANUAL-SCHEMA-v0.1.md`
- `MASTER_BLUEPRINT.md`

Those documents remain governing.

This brief does not modify them.

## 4. First implementation incision

Create a minimal UREEL Unreal proof chamber skeleton on the selected worker machine, with no production secrets, no deployment, no Pixel Streaming, no cloud provisioning, no remote Git write access, and no public release.

The skeleton may include only:

- Unreal project shell
- one empty proof chamber level
- one placeholder UWebBrowser surface or documented bridge placeholder
- one local README / implementation report
- one screenshot or render artifact
- one verification transcript
- no production data
- no credentials
- no public deployment

This incision is a proof chamber skeleton only.

It is not a product.

It is not a public launch.

It is not the full UREEL realm.

It is not a Bubble implementation.

It is not a backend integration.

## 5. Allowed machine boundary

- The selected worker may be a dedicated Windows workstation or Shadow/Parsec-style remote workstation.
- Local Mac remains command console.
- External SSD may be used only for storage relief.
- Cloud GPU / EC2 remains deferred.
- Raspberry Pi / Compute Module remains rejected.
- No worker execution begins from this brief alone.

The worker may carry Unreal pressure only after Human_TURN separately accepts this brief and authorizes the first implementation incision.

The Local Mac remains the authority surface for doctrine, proof review, Git review, and seal decisions.

## 6. Allowed files boundary

Later implementation may create files only inside a future implementation path, not during this capture.

Recommended future path:

```text
projects/ureel-unreal-proof-chamber/
```

This brief does not create that path.

This brief does not create any project file, Unreal file, asset, config, script, README, screenshot, render artifact, verification transcript, build log, or implementation report.

Future implementation must list every created file before seal.

Future implementation must keep generated Unreal project material isolated from RUORA root doctrine.

## 7. No-secret law

1. No production secrets in Unreal files.
2. No service-role keys in project files.
3. No API tokens in Blueprints.
4. No credentials in screenshots, logs, config, assets, or packaged builds.
5. No remote worker receives secrets without separate Human_TURN authorization.
6. No browser projection may expose raw credentials.
7. No deployment keys are authorized.
8. No cloud credentials are authorized.

Secrets are not required for the first proof chamber skeleton.

Any future request for secrets must become a separate Human_TURN authorization gate.

## 8. No-deploy law

- No deployment.
- No Pixel Streaming.
- No public release.
- No CDN.
- No packaged public build.
- No remote Git push.
- No production backend connection.
- No Bubble production connection.
- No payment/auth integration.

The first implementation incision must remain local or worker-local candidate work only.

## 9. Artifact boundary

Future implementation artifacts must return to repo review as candidate evidence only.

Candidate artifacts may include:

- screenshot
- render still
- local video capture
- implementation README
- verification transcript
- git diff
- local build log
- risk report

Worker output is candidate evidence, not proof, until reviewed and sealed by Human_TURN.

Artifact review must distinguish:

- generated worker output
- repo-tracked evidence
- screenshots or render stills
- verification transcript
- implementation claims
- sealed proof

No artifact may be treated as proof merely because the worker produced it.

## 10. Rollback expectations

- Implementation must be removable without damaging RUORA root doctrine.
- Any future project shell must be isolated.
- Any generated files must be listed.
- Any installed software must be reported.
- Any worker-local state must be documented.
- No irreversible deployment is allowed.
- No external account mutation is allowed.

Future implementation must include a rollback path before execution.

Rollback must identify:

- files to remove
- generated artifacts to discard
- worker-local state to clean
- software installed or modified
- accounts touched, if any
- repo status before and after cleanup

If rollback cannot be described, the implementation pass must not begin.

## 11. Future verification requirements

Minimum proof for a later implementation pass:

1. selected machine identified
2. Unreal version identified
3. project path identified
4. created files listed
5. no secrets found
6. no deployment occurred
7. UWebBrowser bridge placeholder or surface documented
8. screenshot/render artifact produced
9. implementation report written
10. rollback path documented
11. final git status reported

Verification must be command-backed or artifact-backed.

Claims about Unreal, worker state, generated files, or artifacts must not be fabricated.

If a command cannot run on the Local Mac because the worker is remote, the implementation report must state where it ran and what evidence returned.

## 12. UWebBrowser boundary

UWebBrowser may appear only as:

- a placeholder surface
- a documented bridge placeholder
- a non-authoritative presentation bridge

This brief does not authorize UWebBrowser implementation.

UWebBrowser is not canonical truth.

It does not replace:

- database truth
- AuditEvent proof
- RealityEvent sealing
- AECHO continuity
- server authorization
- Human_TURN review

## 13. UREEL boundary

UREEL remains projection-only.

The first proof chamber skeleton may demonstrate a future projection surface, but it cannot invent truth.

It must remain downstream of:

- canonical proof events
- JourneyFrame generation
- AuditEvent proof
- RealityEvent sealing
- TransmutationEvent claims
- AECHO continuity

The skeleton may represent a chamber.

It may not claim that the chamber is product-ready.

## 14. Remote worker governance

Remote worker builds.

Human_TURN governs.

Repo proves.

UREEL projects.

Unreal embodies.

The worker must not govern doctrine, proof, Git authority, deployment authority, or launch claims.

No remote Git write access is authorized by this brief.

No remote push is authorized by this brief.

No worker deployment is authorized by this brief.

## 15. UNREAL-002 treatment

This brief does not complete UNREAL-002. UNREAL-002 may be marked complete only after Human_TURN accepts this authorization brief and explicitly authorizes the first implementation incision.

Until then:

- UNREAL-002 remains incomplete.
- MASTER_BLUEPRINT must remain unchanged by this capture.
- No implementation path is created.
- No worker execution begins.
- No Unreal project exists from this pass.

## 16. Launch boundary

- This is not product launch.
- This is not public deployment.
- This is not a service launch.
- This is not Pixel Streaming.
- This is not a cloud build.
- This is not remote execution.
- This is authorization doctrine only.

The launch gate remains closed until Human_TURN explicitly opens the implementation incision.

## 17. Human_TURN review questions

Before UNREAL-002 can be marked complete, Human_TURN should confirm:

1. Which selected worker will be used first: dedicated Windows workstation or Shadow/Parsec-style remote workstation?
2. Is the future implementation path acceptable?
3. Is the first skeleton limited enough?
4. Is UWebBrowser placeholder-only for the first incision?
5. Are secrets fully excluded?
6. Are deployment and Pixel Streaming fully blocked?
7. Is the rollback requirement strict enough?
8. Is the evidence package sufficient for review?

If any answer is uncertain, UNREAL-002 should remain blocked.

## 18. Next gate

Next gate after this capture:

Human_TURN review of this brief.

If accepted, the brief may be sealed.

Only after sealing may Human_TURN decide whether to mark UNREAL-002 complete in `MASTER_BLUEPRINT.md`.

No build begins from this capture.
